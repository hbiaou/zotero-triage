---
phase: 15-content-extraction-&-classification-pipeline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/classification/types.ts
  - src/classification/domain-hints.ts
  - src/classification/domain-classifier.ts
autonomous: true

must_haves:
  truths:
    - "System classifies items into Academic, Software, Farming, or General domains based on item type and content"
    - "Articles, Books, Theses, Reports always classified as Academic regardless of content"
    - "Other item types (Webpage, Video) classified by title/tags/abstract analysis"
    - "Classification includes confidence score (0.0-1.0) indicating certainty"
  artifacts:
    - path: "src/classification/types.ts"
      provides: "Classification types (Domain, ClassificationResult)"
      exports: ["Domain", "ClassificationResult"]
    - path: "src/classification/domain-hints.ts"
      provides: "Item type to domain mapping (hard overrides)"
      exports: ["DOMAIN_HINTS", "getDomainFromItemType"]
      contains: "journalArticle"
    - path: "src/classification/domain-classifier.ts"
      provides: "Domain classification service with LLM-based content analysis"
      exports: ["DomainClassifier"]
      min_lines: 150
  key_links:
    - from: "src/classification/domain-classifier.ts"
      to: "src/services/ai-service.ts"
      via: "AIService.complete() for LLM classification"
      pattern: "aiService\\.complete"
    - from: "src/classification/domain-classifier.ts"
      to: "src/classification/domain-hints.ts"
      via: "getDomainFromItemType() for hard overrides"
      pattern: "getDomainFromItemType"
    - from: "src/classification/domain-classifier.ts"
      to: "src/ai/types.ts"
      via: "EvidenceExtraction for content analysis"
      pattern: "EvidenceExtraction"
---

<objective>
Establish domain classification system that intelligently categorizes Zotero items into Academic, Software, Farming, or General domains using item type priority and LLM-based content analysis with confidence scoring.

Purpose: Enable template-based enrichment by determining which domain-specific template (Academic, Software, Farming, General) should be used for each item during the Accept workflow.

Output: Working classification service with hard overrides for scholarly item types and LLM-based classification for unstructured types, returning confidence scores for override decision logic.
</objective>

<execution_context>
@C:\Users\Biaou\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Biaou\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-content-extraction-&-classification-pipeline/15-CONTEXT.md
@.planning/phases/15-content-extraction-&-classification-pipeline/15-RESEARCH.md
@.planning/phases/14-ai-service-layer-and-evidence-foundation/14-06-SUMMARY.md

# Existing services and types
@src/ai/types.ts
@src/services/ai-service.ts
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create classification types and domain definitions</name>
  <files>src/classification/types.ts</files>
  <action>
Create TypeScript types for domain classification with confidence scoring.

Define:
- `Domain` type: 'Academic' | 'Software' | 'Farming' | 'General'
- `ClassificationResult` interface:
  - domain: Domain
  - confidence: number (0.0-1.0, where 1.0 is certain)
  - reasoning: string (explanation of classification for debugging/logging)
  - isHardOverride: boolean (true if determined by item type, false if by content)
  - sources: string[] (what was analyzed: ['item_type'], ['title', 'abstract'], etc.)

Include comprehensive JSDoc comments documenting:
- Domain definitions (what each domain represents)
- Confidence interpretation (0.7+ is high confidence per research)
- Hard override vs soft classification distinction
- Usage context (Accept workflow, template selection)

Follow existing type patterns from src/ai/types.ts for consistency.
  </action>
  <verify>npx tsc --noEmit (no compilation errors)</verify>
  <done>Types file exists with Domain and ClassificationResult exported and documented</done>
</task>

<task type="auto">
  <name>Task 2: Create item type to domain mapping (hard overrides)</name>
  <files>src/classification/domain-hints.ts</files>
  <action>
Create domain hint system for item type-based hard classification overrides.

Implement:
1. DOMAIN_HINTS constant mapping Zotero item types to domains:
   ```typescript
   export const DOMAIN_HINTS: Record&lt;string, Domain | undefined&gt; = {
     'journalArticle': 'Academic',
     'book': 'Academic',
     'bookSection': 'Academic',
     'thesis': 'Academic',
     'report': 'Academic',
     'conferencePaper': 'Academic',
     // Other types: undefined (allow soft classification)
   };
   ```

2. getDomainFromItemType(itemType: string): Domain | null function:
   - Look up itemType in DOMAIN_HINTS
   - Return domain if found, null otherwise
   - Include JSDoc explaining hard override logic

3. Add helper functions:
   - isAcademicItemType(itemType: string): boolean - check if type forces Academic domain
   - requiresContentClassification(itemType: string): boolean - check if needs LLM analysis

Include comprehensive documentation:
- Why certain item types force Academic (scholarly nature, publication standards)
- Which item types allow soft classification (Webpage, Video, BlogPost, etc.)
- Decision rationale per Phase 15 context (item type priority signal)

Reference existing Zotero item types from src/types.ts (ZoteroItem interface).
  </action>
  <verify>npx tsc --noEmit && grep "journalArticle.*Academic" src/classification/domain-hints.ts (hard overrides defined)</verify>
  <done>Domain hints mapping exists with Academic overrides for scholarly item types, helper functions for classification decision logic</done>
</task>

<task type="auto">
  <name>Task 3: Implement LLM-based domain classifier service</name>
  <files>src/classification/domain-classifier.ts</files>
  <action>
Create DomainClassifier service integrating hard overrides and LLM-based content analysis.

Implement DomainClassifier class:

**Constructor:**
- Takes AIService dependency (from Phase 14-06)

**Public method: async classify(item: ZoteroItem, evidence: EvidenceExtraction): Promise&lt;ClassificationResult&gt;**
1. Check for hard override:
   - Call getDomainFromItemType(item.itemType)
   - If domain found, return ClassificationResult with confidence=1.0, isHardOverride=true, reasoning="Item type forces Academic classification"
2. Otherwise, perform content-based classification:
   - Call private classifyByContent(item, evidence)
   - Return result with isHardOverride=false

**Private method: async classifyByContent(item: ZoteroItem, evidence: EvidenceExtraction): Promise&lt;ClassificationResult&gt;**
1. Build classification prompt:
   - System prompt: Define 4 domains with clear boundaries (see research for examples)
     - Academic: Research papers, textbooks, scholarly articles, technical reports
     - Software: Code, documentation, dev tools, programming libraries
     - Farming: Agriculture, crop science, farming practices, agronomy
     - General: News, blogs, miscellaneous content
   - User prompt: Include title, authors, item type, year, tags (if available), evidence content (first 500 words if FullText, full if Abstract)
   - Instruction: "Respond with JSON: { \"domain\": \"Domain\", \"confidence\": 0.95, \"reasoning\": \"...\" }"

2. Call AIService:
   - Use aiService.complete() with temperature=0.3 (low for consistent classification)
   - maxTokens=200 (classification is brief)

3. Parse response:
   - Extract JSON from response (LLM may wrap with text)
   - Validate with Zod schema (ClassificationResponseSchema)
   - Normalize domain string ("academic" → "Academic", "research" → "Academic")
   - Clamp confidence to 0.0-1.0 range

4. Error handling:
   - On LLM failure or parse error: Return fallback ClassificationResult with domain='General', confidence=0.3, reasoning="Classification failed, defaulting to General"
   - Log errors to console.error for debugging

**Private method: parseClassificationResponse(content: string): ClassificationResult**
- Regex extract JSON from LLM response
- Parse with JSON.parse()
- Validate structure (domain, confidence, reasoning fields)
- Normalize domain value (lowercase → titlecase, synonyms → canonical)
- Return structured result or throw error for fallback handling

**Private method: buildClassificationPrompt(item: ZoteroItem, evidence: EvidenceExtraction): string**
- Include available metadata: title, authors, item type, year, tags
- Include evidence snippet: First 500 words if FullText/Notes, full if Abstract
- Format as structured input for LLM classification

Use Zod for JSON schema validation (already in package.json from Phase 14).
Follow service patterns from src/services/ai-service.ts and src/services/evidence-extractor.ts.
Include comprehensive JSDoc comments documenting classification logic and confidence interpretation.
Handle edge cases: Missing evidence (use metadata only), malformed item data, LLM timeout.
  </action>
  <verify>npx tsc --noEmit (no compilation errors, all imports valid)</verify>
  <done>DomainClassifier service exists with hard override check and LLM-based content classification, confidence scoring, and error handling</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. TypeScript compilation succeeds with no errors
2. Domain types defined with Academic, Software, Farming, General options
3. Hard overrides defined for journalArticle, book, thesis, report, conferencePaper → Academic
4. DomainClassifier integrates AIService from Phase 14
5. Classification returns confidence scores (0.0-1.0)
6. Error handling provides fallback to General domain on classification failure
7. All types and services properly exported
</verification>

<success_criteria>
- [ ] journalArticle items always classified as Academic with confidence=1.0
- [ ] Webpage/Video items classified by content analysis with variable confidence
- [ ] Classification includes reasoning field for debugging
- [ ] Low confidence classifications (< 0.7) can be detected for override modal
- [ ] Classification failures gracefully fall back to General domain
</success_criteria>

<output>
After completion, create `.planning/phases/15-content-extraction-&-classification-pipeline/15-02-SUMMARY.md` following summary template.
</output>
