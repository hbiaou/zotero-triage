---
phase: 15-content-extraction-&-classification-pipeline
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/notes/templates.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Four domain-specific template variants exist (ACADEMIC, SOFTWARE, FARMING, GENERAL)"
    - "Each template includes appropriate frontmatter with domain-specific fields"
    - "Each template contains section headings tailored to the domain"
    - "Templates are accessible via getDomainTemplate(domain: Domain) function"
  artifacts:
    - path: "src/notes/templates.ts"
      provides: "Template variants for all 4 domains"
      exports: ["getDomainTemplate"]
  key_links:
    - from: "future enrichment flow"
      to: "src/notes/templates.ts"
      via: "getDomainTemplate() call"
      pattern: "getDomainTemplate\\(domain\\)"
---

<objective>
Close GAP 1: Missing Domain-Specific Templates

This plan integrates the four user-provided domain-specific templates (Academic, Software, Farming, General) into the codebase as TypeScript functions, enabling template-based enrichment based on domain classification.

Purpose: Templates are required for domain-specific enrichment. Each domain needs its own template structure to preserve domain-specific context (e.g., Methods/Results for Academic, Architecture/Code for Software, Step-by-Step Practices for Farming).

Output: src/notes/templates.ts with getDomainTemplate() function and four template generators; user-provided templates integrated WITHOUT modification.
</objective>

<execution_context>
User has provided four finalized templates in tmp/templates/:
- ACADEMIC_TEMPLATE.md (2959 bytes)
- SOFTWARE_TUTORIAL_TEMPLATE.md (1838 bytes)
- FARMING_TUTORIAL_TEMPLATE.md (1700 bytes)
- GENERAL_TEMPLATE.md (1054 bytes)

Templates are FINAL and approved. Integrate as-is without modification.

Reference: Verification report identifies this as CRITICAL gap blocking phase completion.
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Verification report (2026-02-01):
- Gap 1: Missing domain-specific template variants (CRITICAL)
- Templates are the blocker for template-based enrichment
- All other components (Classification, Modal, DiagnosticNotes) are ready
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add template variant functions for all 4 domains</name>
  <files>src/notes/templates.ts</files>
  <action>
Add four template variant functions to src/notes/templates.ts:

1. generateAcademicTemplate(item: ZoteroItem): string
   - Load from tmp/templates/ACADEMIC_TEMPLATE.md
   - Replace {{Title}} placeholder with item.title
   - Preserve exact structure, all sections (Abstract, Context, Objectives, Niche, Connections, Implications, Speculation, Emphasis, References to Check Out)
   - Include exact frontmatter with domain: Academic

2. generateSoftwareTemplate(item: ZoteroItem): string
   - Load from tmp/templates/SOFTWARE_TUTORIAL_TEMPLATE.md
   - Replace {{Title}} placeholder with item.title
   - Preserve exact structure, all sections (Overview, Context, Objectives, Core Concepts, Procedures/Workflow, Code/Commands, Constraints, Implications, Open Issues, Key Takeaways)
   - Include exact frontmatter with domain: Software

3. generateFarmingTemplate(item: ZoteroItem): string
   - Load from tmp/templates/FARMING_TUTORIAL_TEMPLATE.md
   - Replace {{Title}} placeholder with item.title
   - Preserve exact structure, all sections (Context, Objectives, System Components, Step-by-Step Practices, Timing, Outcomes, Variations, Limitations, Key Takeaways)
   - Include exact frontmatter with domain: Farming

4. generateGeneralTemplate(item: ZoteroItem): string
   - Load from tmp/templates/GENERAL_TEMPLATE.md
   - Replace {{Title}} placeholder with item.title
   - Preserve exact structure, all sections (Overview, Key Ideas, Structure, Notable Statements, Implications, Limitations, Takeaways)
   - Include exact frontmatter with domain: General

Use exact frontmatter from templates as-is. DO NOT modify template content.
  </action>
  <verify>
grep -n "generateAcademicTemplate\|generateSoftwareTemplate\|generateFarmingTemplate\|generateGeneralTemplate" src/notes/templates.ts
Should find all 4 functions defined with proper signatures.
  </verify>
  <done>
All four domain-specific template generators are exported from templates.ts with correct signatures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add getDomainTemplate() function and Domain type</name>
  <files>src/notes/templates.ts</files>
  <action>
1. Define Domain type at top of templates.ts:
   export type Domain = 'Academic' | 'Software' | 'Farming' | 'General';

2. Create getDomainTemplate function:
   export function getDomainTemplate(domain: Domain, item: ZoteroItem): string {
     switch (domain) {
       case 'Academic':
         return generateAcademicTemplate(item);
       case 'Software':
         return generateSoftwareTemplate(item);
       case 'Farming':
         return generateFarmingTemplate(item);
       case 'General':
         return generateGeneralTemplate(item);
       default:
         return generateGeneralTemplate(item); // fallback
     }
   }

This function will be called from enrichment workflows to select template by domain.
  </action>
  <verify>
grep -n "export type Domain\|export function getDomainTemplate" src/notes/templates.ts
Should find type definition and function export.
  </verify>
  <done>
getDomainTemplate() function exists and routes to correct template based on Domain type. Function is exported for use in workflows.
  </done>
</task>

</tasks>

<verification>
After completion, verify:

1. Four template generators exist:
   ```bash
   grep -c "export function generate.*Template" src/notes/templates.ts
   # Should output: 4
   ```

2. getDomainTemplate function exists and is exported:
   ```bash
   grep "export function getDomainTemplate" src/notes/templates.ts
   # Should find the function
   ```

3. Domain type is exported:
   ```bash
   grep "export type Domain" src/notes/templates.ts
   # Should find the type definition
   ```

4. No syntax errors in TypeScript:
   ```bash
   npx tsc --noEmit src/notes/templates.ts
   # Should complete without errors
   ```
</verification>

<success_criteria>
All four domain-specific templates are accessible via getDomainTemplate(domain, item) function. Templates preserve exact structure from user-provided files. No modifications to template content have been made. Type safety via Domain union type. Function ready for integration into enrichment workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/15-content-extraction-&-classification-pipeline/15-05-SUMMARY.md`
</output>
