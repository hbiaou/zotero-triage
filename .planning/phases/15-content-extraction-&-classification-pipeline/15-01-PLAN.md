---
phase: 15-content-extraction-&-classification-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/extraction/types.ts
  - src/extraction/transcript-extractor.ts
  - src/extraction/youtube-service.ts
  - src/ui/transcript-modal.ts
  - package.json
autonomous: true

must_haves:
  truths:
    - "System auto-fetches YouTube transcripts from URLs in Zotero URL field"
    - "System prompts user for manual transcript paste when automatic extraction fails"
    - "System detects unsupported video platforms and offers manual input"
  artifacts:
    - path: "src/extraction/types.ts"
      provides: "Transcript extraction types (TranscriptExtraction, platform detection)"
      exports: ["TranscriptExtraction", "TranscriptPlatform", "TranscriptExtractionError"]
    - path: "src/extraction/transcript-extractor.ts"
      provides: "Video transcript extraction orchestrator"
      exports: ["TranscriptExtractor"]
      min_lines: 80
    - path: "src/extraction/youtube-service.ts"
      provides: "YouTube-specific transcript fetching via youtube-transcript package"
      exports: ["YouTubeService"]
      min_lines: 50
    - path: "src/ui/transcript-modal.ts"
      provides: "Manual transcript input modal for unsupported platforms"
      exports: ["TranscriptModal"]
      min_lines: 60
    - path: "package.json"
      contains: "youtube-transcript"
  key_links:
    - from: "src/extraction/transcript-extractor.ts"
      to: "src/extraction/youtube-service.ts"
      via: "YouTubeService.fetchTranscript() call for YouTube URLs"
      pattern: "youtubeService\\.fetchTranscript"
    - from: "src/extraction/transcript-extractor.ts"
      to: "TranscriptExtractionError"
      via: "throw error when platform unsupported"
      pattern: "throw new TranscriptExtractionError"
---

<objective>
Establish video transcript extraction capability supporting YouTube automatic fetching with manual fallback for unsupported platforms, integrating with Phase 15 evidence hierarchy.

Purpose: Enable video content (YouTube lectures, talks, tutorials) as evidence source for AI enrichment by extracting transcripts automatically or via user input.

Output: Working transcript extraction service with YouTube support and manual input modal, ready for integration into evidence extractor.
</objective>

<execution_context>
@C:\Users\Biaou\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Biaou\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-content-extraction-&-classification-pipeline/15-CONTEXT.md
@.planning/phases/15-content-extraction-&-classification-pipeline/15-RESEARCH.md
@.planning/phases/14-ai-service-layer-and-evidence-foundation/14-05-SUMMARY.md

# Existing types and patterns
@src/ai/types.ts
@src/services/evidence-extractor.ts
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create transcript extraction types and error handling</name>
  <files>src/extraction/types.ts</files>
  <action>
Create TypeScript types for transcript extraction with platform detection and error handling.

Define:
- `TranscriptPlatform` type: 'youtube' | 'vimeo' | 'manual' | 'unsupported'
- `TranscriptExtraction` interface:
  - platform: TranscriptPlatform
  - transcript: string
  - wordCount: number
  - language?: string
  - source: 'auto' | 'manual'
  - sourceUrl: string
- `TranscriptExtractionError` class extending Error:
  - platform: string
  - requiresManualInput: boolean (true when manual paste needed)
  - message: string

Follow existing error patterns from src/ai/types.ts (AIServiceError) for consistent error handling.
Include JSDoc comments for all types documenting usage and purpose.
  </action>
  <verify>npx tsc --noEmit (no compilation errors)</verify>
  <done>Types file exists with TranscriptExtraction, TranscriptPlatform, and TranscriptExtractionError exported</done>
</task>

<task type="auto">
  <name>Task 2: Implement YouTube transcript extraction service</name>
  <files>src/extraction/youtube-service.ts, package.json</files>
  <action>
Install youtube-transcript package and implement YouTube-specific transcript fetching.

1. Install dependency:
   ```bash
   npm install youtube-transcript
   ```

2. Create YouTubeService class in src/extraction/youtube-service.ts:
   - Constructor: no dependencies (stateless service)
   - detectYouTubeUrl(url: string): boolean - regex check for youtube.com or youtu.be domains
   - async fetchTranscript(url: string): Promise&lt;TranscriptExtraction&gt;
     - Use YoutubeTranscript.fetchTranscript(url) from youtube-transcript package
     - Parse response: Extract text segments, join with spaces
     - Calculate word count: text.split(/\s+/).length
     - Return TranscriptExtraction with platform='youtube', source='auto'
     - On error: throw TranscriptExtractionError with platform='youtube', requiresManualInput=true
     - Include error message context (no captions, API error, etc.)

3. Error handling:
   - Wrap YoutubeTranscript.fetchTranscript in try-catch
   - Log errors with console.error before throwing
   - Provide actionable error messages ("Video has no captions available")

Follow code style from existing services (src/services/evidence-extractor.ts) for consistency.
Include comprehensive JSDoc comments.
  </action>
  <verify>npx tsc --noEmit && npm list youtube-transcript (package installed and types valid)</verify>
  <done>YouTubeService class exists with fetchTranscript method, youtube-transcript in package.json dependencies</done>
</task>

<task type="auto">
  <name>Task 3: Create transcript extractor orchestrator and manual input modal</name>
  <files>src/extraction/transcript-extractor.ts, src/ui/transcript-modal.ts</files>
  <action>
Create transcript extraction orchestrator and manual input modal UI.

**File 1: src/extraction/transcript-extractor.ts**

TranscriptExtractor class:
- Constructor: takes YouTubeService dependency
- private detectPlatform(url: string): TranscriptPlatform
  - Check URL regex for youtube.com|youtu.be → 'youtube'
  - Check URL regex for vimeo.com → 'vimeo'
  - Otherwise → 'unsupported'
- async extractTranscript(url: string): Promise&lt;TranscriptExtraction&gt;
  - Detect platform from URL
  - If 'youtube': Call youtubeService.fetchTranscript(url)
  - If 'vimeo' or 'unsupported': throw TranscriptExtractionError with requiresManualInput=true, appropriate message
  - Include error context: "Vimeo transcripts must be manually provided" or "Platform not supported for automatic extraction"

**File 2: src/ui/transcript-modal.ts**

TranscriptModal class extending Obsidian Modal:
- Constructor: takes App, ZoteroItem, onConfirm callback, onCancel callback
- onOpen():
  - Display item title and URL
  - Large textarea for transcript paste (placeholder: "Paste video transcript here...")
  - Character count indicator below textarea
  - Save button (disabled if textarea empty)
  - Cancel button
  - Help text: "Automatic transcript extraction not available. Please paste transcript manually from [platform] or skip enrichment."
- onClose(): Clean up containerEl
- Save button onClick: Call onConfirm(transcript), close modal
- Cancel button onClick: Call onCancel(), close modal

Follow Obsidian Modal patterns from existing codebase (check src/ui/ for modal examples).
Use containerEl.createEl() pattern, not React.
Include accessibility attributes (labels, ARIA roles).
  </action>
  <verify>npx tsc --noEmit (no compilation errors, all types valid)</verify>
  <done>TranscriptExtractor orchestrator with platform detection and YouTubeService integration exists; TranscriptModal provides manual input UI</done>
</task>

</tasks>

<verification>
After all tasks complete:

1. TypeScript compilation succeeds with no errors
2. youtube-transcript package installed in package.json
3. TranscriptExtractor can detect YouTube URLs and fetch transcripts automatically
4. TranscriptExtractor throws appropriate errors for unsupported platforms
5. TranscriptModal provides user interface for manual transcript input
6. All types exported and importable from src/extraction/types.ts
</verification>

<success_criteria>
- [ ] User provides YouTube URL → System auto-fetches transcript without user intervention
- [ ] User provides Vimeo or unsupported URL → System throws TranscriptExtractionError with requiresManualInput=true
- [ ] Manual transcript modal accepts pasted text and passes to callback
- [ ] Error messages are actionable and user-friendly
- [ ] Code follows existing service patterns from Phase 14
</success_criteria>

<output>
After completion, create `.planning/phases/15-content-extraction-&-classification-pipeline/15-01-SUMMARY.md` following summary template.
</output>
