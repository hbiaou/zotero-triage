---
phase: 06-tag-infrastructure-&-extraction
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Tag extraction works with real Zotero 7 database containing annotation tags"
    - "Annotation tags are excluded from tag arrays (custom-color-*, highlight-*)"
    - "Items without tags return empty arrays (no crashes or null values)"
    - "Profile initialization captures tags from seed papers correctly"
    - "v1.0 profiles load without errors in v1.1 (backward compatibility verified)"
  artifacts: []
  key_links: []
---

<objective>
Verify tag infrastructure with real Zotero 7 database, confirming annotation filtering, defensive NULL handling, and profile integration work correctly in production environment.

Purpose: Automated testing can't replicate Zotero's real schema variations or annotation tag patterns. Human verification with actual Zotero library ensures tag extraction is robust and Phase 7 will receive clean tag data.

Output: Confirmation that tag extraction, filtering, and profile integration work correctly with real data. Identified edge cases (if any) documented for Phase 7.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-tag-infrastructure-&-extraction/06-CONTEXT.md
@.planning/phases/06-tag-infrastructure-&-extraction/06-01-SUMMARY.md
@.planning/phases/06-tag-infrastructure-&-extraction/06-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Tag extraction with annotation filtering, defensive NULL handling, schema validation, and profile integration (Plans 06-01 and 06-02).

Changes made:
- ITEM_TAGS_QUERY filters custom-color-*, highlight-*, annotation-*, _* patterns
- Tag extraction in loadItems() has defensive NULL checks and try/catch
- validateTagSchema() checks itemTags and tags tables on connect
- ProfileInitializer extracts tags from seed papers into UserProfile.tags Map
  </what-built>
  <how-to-verify>
**Pre-verification setup:**
1. Ensure you have a Zotero 7 library with:
   - Items WITH user-created tags (at least 5-10 items)
   - Items WITHOUT tags (some untagged items)
   - Items WITH annotation tags (open a PDF, add highlights to generate custom-color-* tags)

**Test 1: Tag extraction with annotation filtering**
1. Open plugin in dev mode (npm run dev)
2. Open Developer Console (Ctrl+Shift+I in Obsidian)
3. Configure plugin with Zotero database path (Settings -> Zotero Triage)
4. Click "Test Connection" button
5. Verify console output:
   - "Tag schema validation issues" warning appears ONLY if itemTags/tags tables missing (should not appear for Zotero 7)
   - No "Failed to extract tags" errors
   - Connection succeeds with item count

6. Run setup wizard (Command palette -> Zotero Triage: Start Setup)
7. Select 5-10 seed papers that have tags
8. Complete wizard
9. Check Developer Console:
   - No errors during profile initialization
   - Profile saved successfully

10. Inspect plugin data.json (in vault's .obsidian/plugins/zotero-triage/ folder):
   - Open data.json in text editor
   - Find userProfile.tags object
   - Verify tags appear as {"tag-name": frequency} (e.g., {"machine-learning": 3})
   - Verify NO annotation tags appear (no custom-color-*, highlight-*, annotation-*)

**Test 2: Items without tags**
1. In Obsidian, open a daily batch (Command palette -> Zotero Triage: Generate Batch)
2. Inspect items in triage view
3. Items without tags should display without errors (no null/undefined crashes)

**Test 3: Defensive NULL handling**
1. Check Developer Console for debug logs:
   - If any "Tag row null for item X, skipping" messages appear: NOTE item IDs
   - These indicate schema variations handled gracefully (expected, not error)

**Test 4: Backward compatibility (if v1.0 profile exists)**
1. If you have a v1.0 profile saved from before Phase 6:
   - Reload plugin (disable/enable in Community Plugins)
   - Open Developer Console
   - Verify profile loads without errors
   - Profile without tags field should work (fallback to empty Map)

**Expected outcomes:**
- ✅ Tags extract successfully from seed papers
- ✅ Annotation tags excluded from profile.tags
- ✅ Items with no tags display correctly (empty array, not null)
- ✅ Schema validation passes for Zotero 7 database
- ✅ v1.0 profiles load without errors

**Failure indicators:**
- ❌ Annotation tags (custom-color-*, highlight-*) appear in profile.tags
- ❌ "Failed to extract tags" errors in console
- ❌ Plugin crashes when loading items without tags
- ❌ Schema validation fails for valid Zotero 7 database
- ❌ v1.0 profile migration causes errors
  </how-to-verify>
  <resume-signal>
Type "approved" if all tests pass and tag infrastructure works correctly.

If issues found, describe:
- Which test failed
- Error messages from console
- Item IDs with problems (if applicable)
- Whether annotation tags appeared in profile.tags
  </resume-signal>
</task>

</tasks>

<verification>
Human verification confirms:
1. Tag extraction works with real Zotero 7 database
2. Annotation tags filtered correctly (custom-color-*, highlight-* excluded)
3. Items without tags handled gracefully (no crashes)
4. Profile initialization captures tags from seed papers
5. v1.0 profiles load without errors (backward compatible)

Edge cases documented for Phase 7 (if any discovered during testing).
</verification>

<success_criteria>
- Tag extraction verified with real Zotero 7 database containing annotation tags
- Annotation filtering confirmed (no custom-color-*, highlight-* in profile.tags)
- Defensive NULL handling prevents crashes for items without tags
- Profile.tags Map contains user-created tags with frequency counts
- Schema validation passes for Zotero 7, logs warning for missing tables
- v1.0 profiles load in v1.1 without errors (backward compatibility verified)
- No breaking changes to existing functionality (batch generation, triage, note creation still work)
</success_criteria>

<output>
After completion, create `.planning/phases/06-tag-infrastructure-&-extraction/06-03-SUMMARY.md`

Document any edge cases discovered during testing (unusual tag patterns, schema variations, migration issues) for Phase 7 reference.
</output>
