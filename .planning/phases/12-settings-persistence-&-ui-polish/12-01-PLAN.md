---
phase: 12-settings-persistence-&-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types.ts
  - src/ui/setup-wizard-modal.ts
  - src/profile/profile-service.ts
  - src/profile/profile-initializer.ts
autonomous: true

must_haves:
  truths:
    - "Relevance vs Diversity setting chosen in wizard persists to plugin settings"
    - "Recency boost setting chosen in wizard persists to plugin settings"
    - "Library filter mode can be configured via settings (foundation for UI in plan 02)"
    - "Settings persist across plugin reloads"
    - "Profile creation uses settings values instead of transient wizard data"
  artifacts:
    - path: "src/types.ts"
      provides: "Settings interface with recommendation preferences"
      contains: "relevanceVsDiversity"
      exports: ["ZoteroTriageSettings"]
    - path: "src/types.ts"
      provides: "Library filter mode enum or type"
      contains: "libraryFilterMode"
    - path: "src/ui/setup-wizard-modal.ts"
      provides: "Wizard saves preferences to settings on completion"
      pattern: "plugin\\.settings\\.relevanceVsDiversity"
    - path: "src/profile/profile-service.ts"
      provides: "Profile creation reads preferences from settings"
      pattern: "settings\\.relevanceVsDiversity"
  key_links:
    - from: "src/ui/setup-wizard-modal.ts"
      to: "plugin.settings.relevanceVsDiversity"
      via: "onComplete callback saves to settings"
      pattern: "plugin\\.settings\\.(relevanceVsDiversity|recencyBoost)"
    - from: "src/profile/profile-initializer.ts"
      to: "settings.relevanceVsDiversity"
      via: "reads from settings instead of wizard params"
      pattern: "settings\\.(relevanceVsDiversity|recencyBoost)"
---

<objective>
Settings persistence infrastructure for recommendation preferences and library filtering.

**Purpose**: Wizard-configured preferences (Relevance vs Diversity, Recency Boost, Library Filter Mode) persist to plugin settings and survive restarts. This enables future UI to edit these settings without re-running the wizard.

**Output**: Settings structure updated, wizard saves preferences on completion, profile creation reads from settings.
</objective>

<execution_context>
@C:\Users\Biaou\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Biaou\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\1GitRepos\zotero-triage\.planning\PROJECT.md
@C:\1GitRepos\zotero-triage\.planning\ROADMAP.md
@C:\1GitRepos\zotero-triage\.planning\STATE.md
@C:\1GitRepos\zotero-triage\.planning\phases\12-settings-persistence-&-ui-polish\12-CONTEXT.md

# Current implementation
@C:\1GitRepos\zotero-triage\src\types.ts
@C:\1GitRepos\zotero-triage\src\profile\types.ts
@C:\1GitRepos\zotero-triage\src\ui\setup-wizard-modal.ts
@C:\1GitRepos\zotero-triage\src\profile\profile-service.ts
@C:\1GitRepos\zotero-triage\src\profile\profile-initializer.ts
</context>

<tasks>

<task type="auto">
  <name>Add recommendation settings to ZoteroTriageSettings interface</name>
  <files>src/types.ts</files>
  <action>
1. Add recommendation preference fields to ZoteroTriageSettings:
   - `relevanceVsDiversity: number` (0-1, default 0 = pure relevance)
   - `recencyBoost: boolean` (default true)
   - `libraryFilterMode: 'personal' | 'all'` (default 'personal')

2. Update DEFAULT_SETTINGS with these defaults:
   - relevanceVsDiversity: 0
   - recencyBoost: true
   - libraryFilterMode: 'personal'

3. Add JSDoc comments explaining:
   - relevanceVsDiversity: "0 = pure relevance (highest scored), 1 = maximum diversity"
   - recencyBoost: "Whether to boost recent publications in recommendations"
   - libraryFilterMode: "Which libraries to include in recommendations (personal = user library only, all = all libraries)"

These fields are currently in UserProfile but need to be in settings for persistence and editability.
  </action>
  <verify>
npm run build succeeds without type errors
grep -n "relevanceVsDiversity\|recencyBoost\|libraryFilterMode" src/types.ts shows new fields in interface and defaults
  </verify>
  <done>
ZoteroTriageSettings interface has recommendation preference fields with documented defaults
  </done>
</task>

<task type="auto">
  <name>Update wizard to save preferences to settings</name>
  <files>src/ui/setup-wizard-modal.ts</files>
  <action>
Modify SetupWizardModal completion flow (where onComplete callback is invoked):

1. Before calling onComplete, save wizard preferences to plugin settings:
   ```typescript
   this.plugin.settings.relevanceVsDiversity = this.wizardData.preferences.relevanceVsDiversity;
   this.plugin.settings.recencyBoost = this.wizardData.preferences.recencyBoost;
   await this.plugin.saveSettings();
   ```

2. Update onComplete callback in both settings.ts call sites to accept UserProfile with seedPaperIds only (preferences come from settings now)

3. Keep wizard UI unchanged - still collect preferences in step 2, but persist them to settings before profile creation

Implementation note: This creates flow where wizard -> settings -> profile (settings become source of truth, not transient wizard data).
  </action>
  <verify>
npm run build succeeds
grep -A5 "onComplete" src/ui/setup-wizard-modal.ts shows settings save before callback
  </verify>
  <done>
Wizard saves relevanceVsDiversity and recencyBoost to plugin settings before completing profile initialization
  </done>
</task>

<task type="auto">
  <name>Update profile creation to read from settings</name>
  <files>src/profile/profile-initializer.ts</files>
  <action>
Modify ProfileInitializer.initializeProfile():

1. Remove `preferences` parameter (was { relevanceVsDiversity, recencyBoost }) - no longer passed from wizard

2. Read from plugin settings instead:
   ```typescript
   const relevanceVsDiversity = this.connector.plugin.settings.relevanceVsDiversity;
   const recencyBoost = this.connector.plugin.settings.recencyBoost;
   ```

3. Use these values when creating profile via profileService.createProfile()

4. Update all call sites:
   - src/settings.ts: Pass only seedPaperIds to initializeProfile
   - src/ui/setup-wizard-modal.ts: Already updated in Task 2

Implementation note: ProfileInitializer constructor needs access to plugin settings (either via connector or direct plugin reference). Check existing pattern in codebase for accessing settings from services.
  </action>
  <verify>
npm run build succeeds
grep -n "initializeProfile" src/profile/profile-initializer.ts shows single parameter (seedPaperIds)
grep -n "settings.relevanceVsDiversity" src/profile/profile-initializer.ts shows settings access
  </verify>
  <done>
Profile creation reads recommendation preferences from plugin settings instead of wizard parameters
  </done>
</task>

</tasks>

<verification>
**Settings persistence:**
1. Run wizard, set relevanceVsDiversity = 0.5, recencyBoost = false
2. Check data.json contains these values in settings (not just in userProfile)
3. Restart plugin (reload Obsidian)
4. Verify settings still contain saved values

**Profile uses settings:**
1. Create profile via wizard with specific preferences
2. Check userProfile.relevanceVsDiversity matches settings.relevanceVsDiversity
3. Manually edit settings.relevanceVsDiversity in data.json
4. Create new profile (clear + re-run wizard)
5. Verify new profile uses updated settings value
</verification>

<success_criteria>
- [ ] ZoteroTriageSettings has relevanceVsDiversity, recencyBoost, libraryFilterMode fields
- [ ] DEFAULT_SETTINGS includes defaults for new fields
- [ ] Wizard saves preferences to plugin settings before calling onComplete
- [ ] ProfileInitializer reads preferences from settings instead of wizard params
- [ ] Settings values persist across plugin restarts (in data.json)
- [ ] Build succeeds with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/12-settings-persistence-&-ui-polish/12-01-SUMMARY.md`
</output>
