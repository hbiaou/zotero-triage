---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - esbuild.config.mjs
  - manifest.json
  - versions.json
  - .gitignore
  - src/main.ts
  - src/types.ts
  - styles.css
autonomous: true

must_haves:
  truths:
    - "npm install completes without errors"
    - "npm run build produces main.js in project root"
    - "Plugin loads in Obsidian without errors (can be tested manually)"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and build scripts"
      contains: "sql.js"
    - path: "src/main.ts"
      provides: "Plugin entry point with onload/onunload"
      exports: ["default"]
    - path: "manifest.json"
      provides: "Obsidian plugin manifest"
      contains: "zotbridge"
  key_links:
    - from: "esbuild.config.mjs"
      to: "src/main.ts"
      via: "entryPoints configuration"
      pattern: "entryPoints.*main\\.ts"
---

<objective>
Initialize the ZotBridge Obsidian plugin project with proper TypeScript configuration, build tooling, and plugin scaffold.

Purpose: Establish a working plugin skeleton that can load in Obsidian and serve as foundation for all subsequent features.
Output: Buildable Obsidian plugin that loads without errors, with sql.js dependency ready.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize project with dependencies</name>
  <files>package.json, tsconfig.json, .gitignore</files>
  <action>
Create package.json with:
- name: "zotbridge"
- version: "0.1.0"
- main: "main.js"
- scripts: "build" (esbuild), "dev" (esbuild --watch)
- dependencies: sql.js@^1.13.0, lodash.debounce@^4.0.8
- devDependencies: @types/node, @types/lodash.debounce, typescript, esbuild, obsidian (latest), builtin-modules

Create tsconfig.json with:
- target: ES2018
- module: ESNext
- moduleResolution: node
- lib: ["DOM", "ES2018"]
- strict: true
- noImplicitAny: true
- outDir: "."
- sourceMap: true
- inlineSourceMap: true
- include: ["src/**/*.ts"]

Create .gitignore with:
- node_modules/
- main.js
- main.js.map
- .DS_Store
- data.json (plugin data)
- *.wasm (will be added separately to releases)

Run `npm install` to install all dependencies.
  </action>
  <verify>
`npm install` completes without errors.
`ls node_modules/sql.js` confirms sql.js is installed.
`ls node_modules/obsidian` confirms obsidian types are available.
  </verify>
  <done>package.json exists with all dependencies, npm install succeeds, node_modules populated</done>
</task>

<task type="auto">
  <name>Task 2: Create build configuration and plugin manifest</name>
  <files>esbuild.config.mjs, manifest.json, versions.json</files>
  <action>
Create esbuild.config.mjs with:
- entryPoints: ["src/main.ts"]
- bundle: true
- external: ["obsidian", "electron", ...builtins] (use builtin-modules package)
- format: "cjs"
- target: "es2018"
- outfile: "main.js"
- sourcemap: "inline"
- platform: "node"
- Read --production flag from process.argv to control minification

Create manifest.json with:
- id: "zotbridge"
- name: "ZotBridge"
- version: "0.1.0"
- minAppVersion: "1.0.0"
- description: "Progressive Zotero-Obsidian bridge for sustainable literature processing"
- author: "ZotBridge"
- isDesktopOnly: true (requires Node.js fs for SQLite access)

Create versions.json with:
- "0.1.0": "1.0.0"
  </action>
  <verify>
File `esbuild.config.mjs` exists and imports correctly.
File `manifest.json` is valid JSON with required fields.
  </verify>
  <done>Build config ready, manifest valid, versions.json present</done>
</task>

<task type="auto">
  <name>Task 3: Create plugin entry point and core types</name>
  <files>src/main.ts, src/types.ts, styles.css</files>
  <action>
Create src/types.ts with TypeScript interfaces:
- ZotBridgeSettings: { zoteroDbPath: string, outputFolder: string }
- DEFAULT_SETTINGS constant with zoteroDbPath: "", outputFolder: "10_Literature"
- ZoteroItem interface: itemID, itemKey, title, authors (string[]), year, doi, journal, volume, pages, abstract, pdfPath, itemType, dateAdded
- RegistryEntry interface: state ('unseen'|'proposed'|'accepted'|'rejected'|'imported'), timestamp
- Registry interface: version, entries (Record<string, RegistryEntry>), lastModified

Create src/main.ts with:
- Import Plugin from 'obsidian'
- Import types from './types'
- Export default class ZotBridgePlugin extends Plugin
- settings property typed as ZotBridgeSettings
- onload(): load settings, log "ZotBridge plugin loaded"
- onunload(): log "ZotBridge plugin unloaded"
- loadSettings(): use loadData() with DEFAULT_SETTINGS fallback
- saveSettings(): use saveData()

Create empty styles.css (placeholder for future UI styling).

Run `npm run build` to verify compilation.
  </action>
  <verify>
`npm run build` succeeds without TypeScript errors.
`main.js` file is generated in project root.
`main.js` contains "ZotBridge plugin loaded" string.
  </verify>
  <done>Plugin compiles, main.js generated, ready to load in Obsidian</done>
</task>

</tasks>

<verification>
1. `npm install` completes without errors
2. `npm run build` produces main.js without errors
3. main.js file exists in project root
4. manifest.json has valid JSON with id "zotbridge"
5. TypeScript types compile without errors
</verification>

<success_criteria>
- Project initialized with all dependencies (sql.js, lodash.debounce, obsidian types)
- Build produces main.js that could load in Obsidian
- Core type definitions ready for subsequent plans
- No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
